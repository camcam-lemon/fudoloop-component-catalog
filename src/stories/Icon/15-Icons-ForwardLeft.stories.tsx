import React from 'react';
import { WHITE } from '../../colors';

import ForwardLeftIcon from '../../components/Icons/ForwardLeft';

export default {
  title: 'Icon/フォワード（左）',
  component: ForwardLeftIcon,
};

export const フォワード左アイコン = () => (
  <div style={{ backgroundColor: WHITE }}>
    <ForwardLeftIcon />
  </div>
);
